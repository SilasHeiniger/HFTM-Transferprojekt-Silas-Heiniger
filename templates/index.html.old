<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding-top: 20px;
            background-color: #f8f9fa;
        }
        .entry-card {
            margin-bottom: 15px;
            transition: transform 0.2s;
        }
        .entry-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .password-field {
            font-family: monospace;
            background-color: #f8f9fa;
            padding: 8px;
            border-radius: 4px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row mb-4">
            <div class="col">
                <h1>🔐 Password Manager</h1>
                <p class="text-muted">Simple password manager application</p>
            </div>
            <div class="col-auto">
                <a href="/folders" class="btn btn-outline-primary me-2">
                    📁 Folders
                </a>
                <a href="/entries/create" class="btn btn-primary">
                    ➕ New Entry
                </a>
            </div>
        </div>

        {% if entries %}
            <div class="row">
                {% for entry in entries %}
                <div class="col-md-6 col-lg-4">
                    <div class="card entry-card">
                        <div class="card-body">
                            <h5 class="card-title">{{ entry.name }}</h5>
                            
                            {% if entry.folder_id %}
                            <p class="card-text">
                                <span class="badge bg-secondary">📁 In Folder</span>
                            </p>
                            {% endif %}
                            
                            {% if entry.username %}
                            <p class="card-text">
                                <strong>Username:</strong><br>
                                <small class="text-muted">{{ entry.username }}</small>
                            </p>
                            {% endif %}
                            
                            {% if entry.website_url %}
                            <p class="card-text">
                                <strong>Website:</strong><br>
                                <a href="{{ entry.website_url }}" target="_blank" class="text-decoration-none">
                                    {{ entry.website_url[:50] }}{% if entry.website_url|length > 50 %}...{% endif %}
                                </a>
                            </p>
                            {% endif %}
                            
                            {% if entry.notes %}
                            <p class="card-text">
                                <strong>Notes:</strong><br>
                                <small>{{ entry.notes[:100] }}{% if entry.notes|length > 100 %}...{% endif %}</small>
                            </p>
                            {% endif %}
                            
                            <div class="mt-3">
                                <a href="/entries/{{ entry.entry_id }}" class="btn btn-sm btn-outline-primary">
                                    View Details
                                </a>
                                <a href="/entries/{{ entry.entry_id }}/edit" class="btn btn-sm btn-outline-secondary">
                                    Edit
                                </a>
                                
                                <form method="POST" action="/entries/{{ entry.entry_id }}/delete" style="display: inline;">
                                    <button type="submit" class="btn btn-sm btn-outline-danger" 
                                            onclick="return confirm('Are you sure you want to delete this entry?')">
                                        Delete
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
        {% else %}
            <div class="alert alert-info">
                <h4>No password entries yet!</h4>
                <p>Click the "New Entry" button to create your first password entry.</p>
            </div>
        {% endif %}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>